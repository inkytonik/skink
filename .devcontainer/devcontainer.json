{
    "name": "Skink",
    "image": "inkytonik/docker-skink:latest",
    "runArgs": [
        "-v", "${env:HOME}${env:USERPROFILE}/.ssh:/root/.ssh",
        "-v", "${env:HOME}/skink:/skink"
    ],
    "extensions": [
        "vscode.git",
        "inkytonik.skink"
    ]
}
